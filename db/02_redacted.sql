-- Core redacted (one row per redacted)
CREATE TABLE redacted (
redacted
);

-- Optional: track raw JSON for diff/debug
CREATE TABLE redacted (
  redacted  INTEGER PRIMARY KEY,
  redacted      JSONB NOT NULL,
  redacted    TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- Helpful indexes
CREATE INDEX ON redacted (redacted);
CREATE INDEX ON redacted (redacted);
CREATE INDEX ON redacted (redacted);
CREATE INDEX ON redacted (redacted);
